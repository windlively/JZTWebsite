<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.xkdjzt.web.dao.IAdministratorDao">
    	<select id="checkAdministrator" parameterType="administrator" resultType="administrator">
    		select * from administrator where adminName = #{adminName} and adminPassword = #{adminPassword}
    	</select>
    </mapper>