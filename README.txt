--项目直接导入Eclipse或IDEA即可运行。
--jzt_db为数据库表结构，使用时需要自行向administrator表中添加一组值，作为管理员账号和密码。
--后台管理系统中上传的图片均存放在项目运行时的服务器的根目录下的JZTResource目录中，但该目录不需要手动创建。
--如果导入了含测试数据的sql文件，默认后台管理账号为："admin"，密码："0000"，并请将本目录下的JZTResource文件夹复制或剪切至运行时的服务器(Tomcat)根目录下。